{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "build": "node_modules/.bin/protocol-buffers ../definitions/polygon.proto -o polygon.js",
    "run": "node client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "google-protobuf": "^3.5.0",
    "grpc": "^1.10.1",
    "protocol-buffers": "^4.0.4",
    "protocol-buffers-encodings": "^1.1.0"
  }
}
